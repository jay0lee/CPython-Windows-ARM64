# This is a basic workflow to help you get started with Actions

name: CI

# Controls when the action will run. Triggers the workflow on push or pull request
# events but only for the master branch
on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:
    runs-on: windows-latest
    strategy:
      matrix:
        arch: [x86, x64, arm, arm64]
        majorpython: ["3.9", "3.10"]

    steps:
      - name: Set environment variables
        env:
          ARCH: ${{ matrix.arch }}
        run: |
          switch ( $ARCH )
          {
            x86
              {
                $installerarch = 'Win32'
                $embeddedarch = ''
              }
            x64
              {
                $installerarch = 'AMD64'
                $embeddedarch = 'ADM64'
              }
            arm
              {
                $installerarch = 'ARM'
                $embeddedarch = 'ARM'
              }
            arm64
              {
                $installerarch = 'ARM64'
                $embeddedarch = 'ARM64'
              }
          }
          Write-Output "ARCH: $env:ARCH"
          Write-Output "installerarch: $env:installerarch"
          Write-Output "embeddedarch: $env:embeddedarch"
          Add-Content -Path $env:GITHUB_ENV -Value "ARCH=${env:ARCH}"
          Add-Content -Path $env:GITHUB_ENV -Value "installer_arch=${env:installer_arch}"
          Add-Content -Path $env:GITHUB_ENV -Value "embedded_arch=${env:embedded_arch}"

      - name: Install 7Zip PowerShell Module
        run: Install-Module 7Zip4PowerShell -Force -Verbose

      - name: Clone Python
        env:
          majorpython: ${{ matrix.majorpython }}
        run: |
          git clone https://github.com/jay0lee/cpython.git
          cd cpython
          git checkout $majorpython

      - name: Build installers
        run: |
          cd cpython
          Tools\msi\buildrelease.bat -$ARCH --out release
          echo "In release:"
          dir release\
          dir release\*\

      - name: upload artifacts
        uses: actions/upload-artifact@v2
        with:
          name: python-${{ env.installer_arch }}.exe
          path: |
            cpython/release/${{ env.installer_arch }}.exe
